name: 打包 Windows 版本

on:
  push:
    branches: [main, master]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 安装依赖
        run: |
          pip install -r requirements.txt

      - name: 打包 EXE
        run: |
          pyinstaller --onefile --name SSS五绝计算器 `
            --add-data "templates;templates" `
            --add-data "static;static" `
            --add-data "fund_tracker.db;." `
            --hidden-import flask_sqlalchemy `
            --hidden-import sqlalchemy.sql.default_comparator `
            --hidden-import sqlalchemy.ext.declarative `
            --hidden-import chinese_calendar `
            --hidden-import chinese_calendar.constants `
            --hidden-import calculator `
            --hidden-import models `
            --hidden-import trading_calendar `
            --exclude-module tkinter `
            --exclude-module matplotlib `
            --exclude-module numpy `
            --exclude-module pandas `
            --console `
            app.py

      - name: 发布到 GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: latest
          name: "SSS五绝计算器 - Windows"
          body: "双击 SSS五绝计算器.exe 即可使用，无需安装任何环境。"
          artifacts: "dist/SSS五绝计算器.exe"
          allowUpdates: true
          makeLatest: true
